# 成果物の受け渡しルール

## 基本方針
- 修正・追加の依頼に対しては、変更があったファイルのみを提示する
- ファイル全体を貼る場合でも、どこを変更したか（新規/修正/削除）を明記する
- `wrangler.toml` は特に注意し、`.claude/rules/wrangler.md` のルールを優先する

## レビューのしやすさ
- 複数ファイルにまたがる変更は、変更点の要約を先頭に1〜3行で書いてから各ファイルを提示する
- `db/schema.sql` の変更は、変更差分を明示する（既存のシード行を無断で削除・変更しない）
