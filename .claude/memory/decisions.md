# 意思決定ログ

過去の重要な決定を日付付きで残す。Claudeが文脈を再学習しなくて済むようにするためのもの。

- AI機能はUI非表示の方針（顧客向けに「AIが自動提案」と見せない）
- 管理画面アクセスは隠しアクセス方式を採用
- 見積もり→ヒアリング→提案依頼書→正式見積書送付、という業務フローに整理（以前はヒアリング後のメール通知がなかったため追加した）
- リポジトリ名は`yoshizaki-studio`、内部コード名（package.json/D1名/Workers名）は`ty-mitumori`のまま維持することに決定
- 2026-08-11: `pricing-config.js`のグラフィック・デザインカテゴリの人日単価を35,000円→45,000円に改定（ユーザー了承済み）。根拠: フリーランス/制作会社のロゴデザイン・Webデザイン等の公開相場情報（ロゴ制作5万〜30万円、Webデザイナー人日単価5万円など）と比較して旧単価が低すぎたため。web(45,000)/video(40,000)/app(65,000)/system(70,000)の各単価は同様の市場調査の結果、概ね妥当と判断し据え置き。単価の妥当性は今後も定期的に見直すこと
- 2026-08-14: `.claude/skills/`にAster Systems個人用スキル一式（開発・デザイン・マーケ等、プロジェクト非依存）を追加。うちdesigners/animation-referencesが紹介する外部デザインツールは、既存の`tokens.css`（navy×gold、2026-08-11確定）と衝突しない「既存UI監査・改善系」のみ選定してインストール（Frontend Design＝Anthropic公式、Taste `redesign-existing-projects`バリアント、apple-design、transitions-dev）。ゼロからデザインシステムを生成するタイプのツール（UI UX Pro Max、Taste `design-taste-frontend`等）は既存ブランド軸と衝突するため見送った
- 2026-08-14: 上記スキルを使い、全8テンプレート（portal/estimate/hearing×5/quote/proposal/mypage/admin）を構成から見直して刷新。ヘッダー/フッター/ツールバーの重複CSSを`public/css/base.css`に一本化。見積計算ロジック（`computeTotal`/`encodeEstimate`/`decodeEstimate`/`buildLineItems`）・`src/index.ts`・`admin.js`・DBスキーマは一切変更していない。code-reviewerエージェントによる独立レビューを実施し、指摘（スマホ幅でのサマリーバーのズレ等）を反映済み。ブラウザのウィンドウリサイズが機能しない開発環境だったため、スマホ幅の最終確認はユーザー実機依存だった
- 2026-08-14: release-check・レビュー通過後は確認を待たずに`npm run deploy`し`.claude/memory/decisions.md`を更新する運用に変更（ユーザー依頼、CLAUDE.md「作業の進め方」に明記）。金額計算・DBスキーマ・認証・wrangler.toml関連の変更は従来通り事前承認が必要
- 2026-08-16: ヒアリングシート5ページ（hearing/hearing_app/hearing_design/hearing_system/hearing_video、共通の`hearing.js`/`hearing-config.js`/`hearing.css`で描画）に、セクション進捗ナビを追加（`public/js/hearing.js`の`renderSectionNav`/`initSectionNav`、`public/css/hearing.css`の`.section-nav`系）。長い1ページ形式のフォームで現在地と入力済みセクションが分かりにくいという課題に対応。ヘッダー直下に固定表示し、クリックで該当セクションへスクロール、入力があると番号を✓に変更、スクロール位置に応じてIntersectionObserverでアクティブ表示を切り替える。見積もりシミュレーターの①②③ステップ表示と同じCSS変数（--navy/--gold/--gold-soft等）を流用し配色を統一。計算ロジック・DBスキーマ・wrangler.toml・admin.js・src/index.tsは無変更。tsc・code-review・release-check通過後、ユーザー承認を得て`npm run deploy`でデプロイ済み（Version ID: cb61c7e6-ac2d-441c-9805-6328c1c66759）。実機（スマホ）でのタップ・スクロール感触は未確認
- 2026-08-16: お客様向け画面で通信・API失敗時に内部情報（`RESEND_API_KEY`/`ANTHROPIC_API_KEY`等の環境変数名、`err.message`由来の英語・技術的なエラー文）がそのまま表示されていた箇所を横断的に修正。対象：`public/js/estimate.js`（見積書メール送信・AIカテゴリ提案）、`public/js/hearing.js`（記入内容チェック）、`public/js/mypage.js`（進捗確認、サーバー側の正規メッセージはそのまま表示し通信エラー時のみ汎用文言に変更）、`src/index.ts`（`/api/estimates/send`・`/api/admin/cases/:id/send-formal-quote`・`/api/ai/suggest-estimate`・`/api/ai/hearing-assist`のエラーメッセージから環境変数名・生の例外文言を除去し`console.warn`でサーバーログに退避）。計算ロジック・DBスキーマ・wrangler.toml・認証は無変更。tsc・code-review・release-check通過後、ユーザーより「今後確認不要」と明示的な事前承認を得たため、以後はこの種の変更（見た目・文言・UX改善で金額計算/DBスキーマ/認証/wrangler.tomlに触れないもの）はrelease-check・レビュー通過後、確認を待たずにデプロイする。`npm run deploy`でデプロイ済み（Version ID: 5236e262-bee1-4441-a94d-8d732281af23）
- 2026-08-16: 見積もりシミュレーターで入力したお名前・メールアドレスを、直後に進むヒアリングシート（基本情報セクション）へ自動引き継ぎするようにした（`public/js/estimate.js`の送信ボタン押下時に`sessionStorage`へ一時保存、`public/js/hearing.js`の`prefillContactFromEstimate`で読み出し）。従来は見積もり送信直後にヒアリングへ進むと同じ名前・メールを再入力させられていた。URLにPIIを載せたくないため、同一タブ内でのみ有効な`sessionStorage`経由（キー: `ty-contact-carryover`）にし、見積もりコード（`?code=`）付きで遷移した場合のみ適用、既に入力済み・下書き復元済みの値は上書きしない。Playwrightで「webカテゴリで入力→ヒアリングへ遷移→自動入力される」「コードなしで直接ヒアリングを開くと空欄のまま」の両方を確認済み。計算ロジック・DBスキーマ・wrangler.toml・認証は無変更。tsc・code-review・release-check通過後、ユーザーの事前包括承認に基づき確認なしで`npm run deploy`済み（Version ID: d631de34-5add-4384-9329-1bad26b508ee）
- 2026-08-16: 見積もり→ヒアリング→マイページ→見積書プレビューの一連の流れを実際に操作して見つかった不具合3件を修正。(1) `public/js/mypage.js`の`categoryLabel`が`window.HEARING_FORMS`を参照していたが、`hearing-config.js`の`const HEARING_FORMS`はトップレベル宣言のため`window`のプロパティにならず常に判定に失敗し、マイページに内部カテゴリID（例:「web」）がそのまま表示されていた。`typeof HEARING_FORMS !== "undefined"`で判定するよう修正し「Webサイト制作」等の日本語ラベルが出るようにした。(2) `public/js/hearing.js`：ヒアリングシート送信完了後、フォーム本体は非表示になるが、直前に追加したセクション進捗ナビ（`#section-nav`、フォームの外側の兄弟要素）が表示されたまま残っていたため、完了時に`section-nav`も`hidden`にするよう修正。(3) `public/css/quote.css`・`public/js/quote.js`：見積書プレビューのスマホ幅で品目テーブルが`display:block`指定により横スクロール前提のレイアウトになっていたが、スクロール発見性が低く「人日数・人日単価・金額」列が画面外に隠れて実質見えなかった。品目ごとにカード状に積む表示へ変更（各`<td>`に`data-label`属性を追加しCSSの`::before`でラベル表示）。`window.print()`をスマホから呼んだ場合に印刷/PDFまでカード表示にならないよう該当スタイルは`@media screen and (max-width: 640px)`に限定し、印刷では従来の表形式を維持することをPlaywrightの`emulate_media(media="print")`で確認済み。`buildLineItems`等の計算ロジック・金額は無変更。tsc・code-review・release-check通過後、ユーザーの事前包括承認に基づき確認なしで`npm run deploy`済み（Version ID: b2a87e28-9d94-4579-9c78-473f1d13727b）
- 2026-08-18: ユーザー提供の個人用スキル`ai-design-polish`（`C:\Users\ytaka\.claude\skills\ai-design-polish\`、プロジェクト非依存）を使い、「AI感のあるデザイン」を避ける観点でUI刷新を実施。手順: (1)全画面監査→紫グラデ等の典型的な"AI感"はほぼ無く、実害はportal.htmlの絵文字3箇所(⏱📝📊)とestimate.htmlのバンドルバッジ絵文字1箇所のみと判明。(2)デザイントークンは新規YAMLを作らず既存`tokens.css`を正本のまま継続（リポジトリ整理ルールに準拠）。(3)参照デザインシステムはAdobe Spectrum（フォーム密度の高い業務ツールとして採用、navy×goldのブランド色自体は維持）。(4)portal.html/estimate.htmlの白黒ワイヤーフレーム3案ずつをArtifactでユーザーに提示し、両ページともB案を採用（portal=タスクファースト型、estimate=ウィザード型）。
  - `public/portal.html`/`public/css/portal.css`: ヒーローを大幅短縮しツール一覧を上部に、「選ばれる理由」セクション（絵文字アイコン使用）を削除し「ご依頼の流れ」の帯に統合。カテゴリツールカードのバッジ色を6色（カテゴリごとに別々の色）から単色gold（`var(--gold-soft)`）に統一。
  - `public/estimate.html`/`public/js/estimate.js`: カテゴリ選択後にカテゴリグリッドを隠し「選択中カテゴリ」バー＋「カテゴリを変更する」ボタンで戻れる簡易ウィザード形式に変更（完全な複数画面遷移ではなく表示制御のみ、リスク低減のため）。`computeTotal`/`encodeEstimate`/`updateSummary`等の金額計算ロジックは一切変更していない。
  - `public/js/pricing-config.js`/`public/js/hearing-config.js`: `badge`/`badgeBg`フィールド（カテゴリバッジの背景色）のみを個別の6色から`var(--gold-soft)`に統一。price/rate/days等の金額フィールドは無変更（`git diff`で確認済み）。
  - `public/css/estimate.css`: 「おトクなセットプラン」バッジのブランド外オレンジ系グラデーション(`#ff9a4d`〜`#ff6a3d`)をgoldトークンに統一。
  - portal.htmlの「6カテゴリ対応」の文言誤り（実際は5カテゴリ）を修正。
  - 実装中にモバイル(390px)実機確認で発見した不具合を修正: portal.htmlの「ご依頼の流れ」ステップ帯が横並びflex-wrapで折り返す際、各ステップの末尾連結線(`::after`)が孤立して見える問題 → 560px以下では縦積みレイアウトに変更。
  - 未対応・スコープ外として残した項目: `pricing-config.js`内の既存オプションラベル「AI字幕生成（多言語対応）」が`.claude/rules/content-policy.md`の「顧客向け文言でAIと明示しない」に抵触（今回のdiff対象外、`git diff`で無変更確認済み）。対応要否はユーザー判断待ち。
  - code-reviewerによる独立レビュー2回・release-check通過（金額計算・DBスキーマ・認証・wrangler.toml・禁止文言への抵触なしを確認）。tsc --noEmitエラーなし。ユーザーの事前包括承認に基づき確認なしで`npm run deploy`済み（Version ID: a02027e5-00e4-4c7c-8916-3b5e353171e6）。デプロイ後は全11画面・関連API4種のHTTP 200を`curl`で確認したが、ブラウザ拡張機能の不具合でChrome経由の本番目視スクリーンショットは取得できなかった（ローカルdev環境では同一コードをPC/スマホ両方視覚確認済み）。
  - 追記: 上記で「未対応」としていた`pricing-config.js`の「AI字幕生成（多言語対応）」ラベルも、id/rate/days（`ai-subtitle`, 40000, 0.8）は変更せずラベル文言のみ「字幕生成（多言語対応）」に修正して追加デプロイ（Version ID: 4f0dbdd4-52d8-4927-960e-ee6c61778fb7）。本番`/js/pricing-config.js`の内容を`curl`で確認し反映済みを確認。ブラウザ自動操作ツールがこのセッション中終始不安定（ローカル・本番問わずスクリーンショット/JS実行がタイムアウト）だったため、視覚的な最終確認は`curl`によるHTTPレスポンス内容の確認で代替した。
- 2026-08-21: 開発ドキュメント生成ツール一式（`docs/generators/`・`docs/assets/`、iCloud側の旧コピーに存在していたもの）をこのリポジトリへ導入し、`package.json`に`docs`/`docs:check`スクリプトと`docx`/`pptxgenjs`devDependenciesを追加した（`docs/`は`.gitignore`対象のまま）。iCloud側コピーは2026-08-05時点の古いコード状態を反映しており、要件定義書等16点の内容が実装と乖離していたため、本リポジトリの現状（bundles/delivery_options/campaigns/pricing_overrides/email_settingsテーブル、下書き自動保存、確認メール・受付番号・マイページ導線、セクション進捗ナビ、見積もり→ヒアリングの入力引き継ぎ、エラー文言の適正化、デザイン刷新等）に合わせて16文書すべての内容を更新（version 1.2、2026年8月）。あわせて見積金額の再計算ロジック（`computeTotal`のパイプライン）とシステム構成を図解した新規資料（`gen_illustrated_guide.js`→`ty-mitumori_見積再計算ロジック_システム構成_図解ガイド.docx`、非エンジニア向けの用語集付き）を追加し、計17文書を`npm run docs`で生成する構成にした。`gen_diagrams.py`の日本語フォント参照をLinux想定からWindows（`C:/Windows/Fonts/meiryo.ttc`等）にも対応させ、新規に見積金額計算パイプライン図（`img_calc_pipeline.png`）を追加。`check_docs.js`が使うpandoc/markitdownは本機に未インストールのため検査は生成元jsソースへの`grep`で代替（禁止ワードなしを確認）。生成した17点は"C:\Users\ytaka\iCloud Drive\iCloudDrive\システム開発\ty-mitumori_開発資料一式(2026年8月版)"にカテゴリ別5フォルダ（要件書・仕様書／設計書／見積書／図解説明資料／運用資料）で整理して格納済み。金額計算ロジック・DBスキーマ・認証・wrangler.tomlは無変更（ドキュメントのみの変更）。
- 2026-08-27: 管理画面に新規ページとして「経営ダッシュボード」（`public/dashboard.html`/`public/js/dashboard.js`）と「アプリ構造ビューアー」（`public/structure.html`/`public/js/structure.js`/`public/js/structure-data.js`）を追加。事前にAskUserQuestionでスコープを確認し、ダッシュボードは案件KPI集計・売上見積金額集計・料金体系/キャンペーン状況一覧の3点、構造ビューアーはアプリ構成の可視化（ページ/API/DBテーブル/業務フロー）と決定。実装方針：(1) 2ページとも`proposal.html`と同じ隠しアクセス方式を踏襲し、`admin.html`の隠しリンク経由でのみ到達する独立HTMLページとして追加（`src/index.ts`の`app.get("*")`が既存のASSETSフォールバックでそのまま配信するため、ルーティング側の変更は不要）。(2) ダッシュボードのKPI・売上集計は新規APIを作らず、既存の`/api/admin/cases`・`/api/admin/campaigns`・`/api/admin/bundles`・`/api/admin/delivery-options`のレスポンスをブラウザ側で集計するのみ（`cases`一覧はAPI側で直近200件までのLIMITがあるため、画面上に「直近200件まで集計」と明記して正確性を担保）。(3) 構造ビューアーのデータは`docs/app-structure.json`（`.gitignore`対象・未デプロイ）をそのまま流用せず、`src/index.ts`の全ルート・`db/schema.sql`の全テーブルを実際に読み直して`public/js/structure-data.js`に手動で書き起こした静的データとした（`docs/`はデプロイされないため）。実装を変更した際はこのファイルも更新する必要がある旨を画面上に明記。(4) 両ページとも認証は`/api/admin/me`が401の場合`/admin.html`へ即リダイレクトする方式（`proposal.html`の「画面内にエラー表示」方式とは異なり、繰り返し使う管理画面としてリダイレクトを採用）。(5) 新規CSSは`public/css/insights.css`（`admin.css`を`@import`し、stat-card/構造ビューアー用テーブル・フローリストのスタイルのみ追加）。`admin.css`の`.settings-btn`に`display:inline-flex`を追加（`<a>`タグでもボタンと同じ見た目になるようにするための後方互換な追加のみ、既存ボタンの見た目は変化なし）。`admin.html`のヘッダーに両ページへのリンクを追加。金額計算ロジック・DBスキーマ・認証・wrangler.tomlは無変更（`src/index.ts`はまったく編集していない）。tsc --noEmit通過。ローカル`wrangler dev`+一時テスト管理者アカウント（動作確認後に`DELETE`済み、本番DBには一切触れていない）でログイン・両ページの表示・全タブ切り替え・ログアウト後のリダイレクトをブラウザ実機確認済み。スマホ幅の確認はブラウザ拡張機能のウィンドウリサイズが本環境で機能しないため未確認（CSSは`admin.css`の既存レスポンシブパターンを踏襲し、`stat-grid`等に`auto-fit`とモバイル用`@media`を追加済み）。
  - 追記：`/code-review medium`で2件の指摘。(1) `admin.html`のヘッダー右側`<div>`（今回2リンクを追加した箇所）に`flex-wrap:wrap`が無く、新規2リンク込みで5項目になった結果スマホ幅で折り返せずオーバーフローする恐れがあるとの指摘 → 該当`<div style="...">`に`flex-wrap:wrap`を追加して対応済み（`dashboard.html`/`structure.html`のヘッダーと同じパターンに揃えた）。(2) `public/css/portal.css`から`.btn-ghost`が削除されているのに`public/404.html`がまだ`btn-ghost`クラスを使っており見た目が崩れるとの指摘 → これは今回のセッションで触れていない、以前からリポジトリに残っていた未コミットの変更（2026-08-18のデザイン刷新以降、portal/estimate周りが未コミットのまま置かれていたもの）が原因であり、今回のダッシュボード/構造ビューアー追加とは無関係。ユーザーに報告済み・対応要否は保留。
  - 追記：ユーザー承認のもと`npm run deploy`でデプロイ済み（Version ID: 9998d979-9418-46f1-89c2-b14e7979a288）。アップロードされたのは今回変更した8ファイルのみ（`insights.css`/`structure.js`/`admin.css`/`dashboard.html`/`dashboard.js`/`admin.html`/`structure.html`/`structure-data.js`）で、他の30ファイルは「already uploaded」と表示され今回のデプロイでは変化なし（リポジトリに残っている2026-08-18/08-21の未コミット変更は、その時点で既に`wrangler deploy`済みで本番に反映されていたことを裏付ける。上記(2)の`.btn-ghost`不具合も今回新たに生じたものではなく、その時点から本番に存在していたと考えられる）。デプロイ後、本番URL（studio.aster-system.com）で新規7アセットのHTTP 200を`curl`で確認済み。
  - 追記：ドキュメント更新も実施。`docs/generators/`の`gen_screen_design.js`（画面設計書：v1.3、8.経営ダッシュボード／9.アプリ構造ビューアーの節を追加）・`gen_sitemap.js`（サイトマップ：v1.3、/dashboard.html・/structure.htmlをadmin.html配下に追記）・`gen_basic_design.js`（基本設計書：v1.3、画面一覧テーブルに2行追加）・`gen_spec.js`（機能一覧書：v1.3、画面一覧テーブルに2行、3.14節を新設）を更新し、`npm run docs`で17文書を再生成（`docs/out/`、docxのXMLに新規文言が反映されていることを確認済み）。更新した4文書（機能一覧書_仕様書／画面設計書／サイトマップ／基本設計書）を"C:\Users\ytaka\iCloud Drive\iCloudDrive\システム開発\yoshizaki-studio\ty-mitumori_開発資料一式(2026年8月版)"内の該当フォルダ（01_要件書・仕様書、02_設計書）に上書きコピーして反映済み。あわせて`docs/app-structure.json`・`docs/app-structure.html`（プロジェクト内リファレンス、非デプロイ）にも2画面を追記し、JSON構文の妥当性を確認済み。`gen_manual_pptx.js`（操作説明書）等、今回の2画面追加で内容が誤りにならない他の生成スクリプトは意図的に未変更。`docs/`は`.gitignore`対象のため、これらの変更はgitステータスに現れない（想定通り）。
  - 追記：iCloud側のドキュメント更新をユーザーに再確認され、`unzip -p *.docx word/document.xml | grep`で「経営ダッシュボード」「dashboard.html」「構造ビューアー」の文言が実際にiCloud側4ファイルへ反映済みであることを再検証（問題なし。おそらくiCloud同期アイコンの見た目で未反映と誤認された）。あわせて残タスク3件に対応。(1) `public/css/portal.css`に`.btn-ghost`を再定義し`public/404.html`の表示崩れを修正・デプロイ済み（Version ID: ce34d8cc-23f4-4e35-9ea0-dd78fe276815）。他画面で`btn-ghost`を使用していないことを確認済み。(2) スマホ幅の実機確認を再試行したが、本環境のブラウザ拡張機能は`resize_window`後に`Page.captureScreenshot`がタイムアウトする既知の制約により今回も不可（未確認のまま）。(3) 未コミットだった全変更をユーザー承認のもと3コミットに分割してコミット（ポータル/見積もりデザイン刷新、docs生成ツール依存関係追加、経営ダッシュボード/構造ビューアー追加）した上で`git push origin main`を実行し、GitHubリモートに反映済み。
    - 補足：ローカル動作確認中、`npm run dev`のデフォルトポート8787が別プロジェクト（`genba-mvp`、ユーザーの別アプリ「現場運営支援システム」）と衝突し、Windowsの`netstat`上はyoshizaki-studio側のworkerdが`Listen`と表示されるにもかかわらず実際のHTTPレスポンスは`genba-mvp`側が応答するという紛らわしい状態を確認した。以後、このプロジェクトのローカル確認時は`npx wrangler dev --port 8790`等、8787以外のポートを明示指定する（`genba-mvp`側のプロセスには一切触れていない）。
- 2026-08-29: ユーザー依頼「フォルダの整理（見やすく・使いやすく）」により`public/`配下を役割別サブフォルダに再編。本来`.claude/rules/architecture.md`の「既存のディレクトリ構成を大きく変えない」方針に反するが、「まだ顧客に公開していないサイト」であり旧URLへのリンクが外部に存在しないことをユーザーに確認の上、ユーザー了承で実施。
  - 変更内容：`public/customer/`（estimate.html・quote.html・mypage.html）、`public/hearing/`（hearing.html・hearing_video.html・hearing_app.html・hearing_system.html・hearing_design.html）、`public/admin/`（admin.html・dashboard.html・structure.html・proposal.html）の3サブフォルダを新設し`git mv`で移動。`portal.html`（起点）・`404.html`（Cloudflare Assetsのフォールバック規約上必須）はルート直下を維持。`css/`・`js/`・`img/`は無変更（HTMLからは元々絶対パスで参照していたため移動の影響なし）。
  - 追随して更新：`src/index.ts`（`/estimate`・`/admin`・`/mypage`のリダイレクト先、`CATEGORY_INFO`の`hearingUrl`、メール本文の`mypageUrl`/`quoteUrl`生成）、`public/js/pricing-config.js`（`hearingUrl`）、`public/js/admin.js`・`dashboard.js`・`structure.js`・`estimate.js`・`mypage.js`（画面遷移先パス）、`public/js/structure-data.js`（構造ビューアーのpages一覧・userFlow表示）、各HTML間の相対/絶対リンク（portal.html・404.html・admin系3ファイル・hearing系5ファイル・quote.html）、`README.md`のパス記載、`.claude/rules/architecture.md`のディレクトリ構成方針、`CLAUDE.md`のページ構成一覧。金額計算ロジック・DBスキーマ・認証・wrangler.tomlは無変更。
  - 検証：`npx tsc --noEmit`エラーなし。`npx wrangler dev --port 8799`を起動し、新パス（`/customer/estimate.html`・`/hearing/hearing*.html`・`/admin/admin.html`他）が全てHTTP 200、`/`・`/admin`・`/estimate`・`/mypage`のリダイレクト先が新パスになっていること、旧パス（例：`/estimate.html`）が意図通り404になることを`curl`で確認済み。ブラウザでの目視確認（PC/スマホ幅）は未実施。
  - あわせて`docs/`（`.gitignore`対象）直下に散らばっていた`app-structure.html`・`app-structure.json`・`wireframe-review.html`を`docs/reference/`にまとめた（`generators/`・`out/`・`assets/`と並列。他ファイルからの参照なしを確認済みのため影響なし）。
  - 残タスク：本番デプロイは未実施（今回はローカル確認のみ）。デプロイ前に旧URL（`/admin.html`等）を誰かがブックマークしていないか再確認し、リリース時は`/project:release-check`を通すこと。
- 2026-08-29: ユーザーから「CLAUDE.md/rules/skills/agents/settings.json+Hooks/Auto memory等の多層制御構成を追加してほしい」との依頼を受け、まず`.claude/`配下を監査したところ、該当する7層はすべて既存で稼働済みと判明（新規作成は行わず重複を回避）。ただし監査の過程で以下の実質的な不備を発見し修正した。
  - `.claude/rules/architecture.md`と`.claude/agents/security-reviewer.md`が、直前の`public/`再編（本ログ前項）前の`proposal.html`という古いパス表記のまま残っていた → `admin/proposal.html`に修正。
  - `.claude/scripts/block_dangerous_bash.py`（PreToolUseフックで危険Bashコマンドをブロックする層）の検知パターンが`rm -rf /`・`git push --force`等最小限のみで、`git reset --hard`・`git clean -f`・`git branch -D`・`rm -rf .`（ルート指定でなくカレント指定）・D1への`DROP TABLE`/WHERE無し`DELETE FROM`が未検知だった → パターンを追加。誤検知がないこと（`npm run dev`・通常の`git push`・`db-migrate.md`が想定する`wrangler d1 execute --remote --file=...`等は非ブロック）をローカルで13ケース実行し確認済み。
  - それ以外（CLAUDE.md本文、`.claude/skills/`各SKILL.md、`.claude/commands/`、`.claude/settings.json`のフック配線、`.claude/memory/known-issues.md`）に実害のある不備は見つからなかった。
- 2026-08-29: ユーザー依頼「依頼が来たら全自動でメール返信・ヒアリング・見積書作成・詳細資料作成まで行うAI機能」を、事前のAskUserQuestionで要件を確認したうえで7フェーズに分けて実装（起点＝サイト操作とメール受信の両方、送信前チェックなしの完全自動、資料はまず社内専用）。全自動送信は既定OFFのフィーチャーフラグとし、ユーザーが明示的にONにするまで既存挙動は変わらない。設計の核：**AIには金額を一切計算させず、AIの出力（カテゴリ・ヒアリング項目id・プラン/オプションid）は必ずホワイトリストと照合し実在しないものを機械的に除去する**（捏造防止）。実装前に3体のExploreエージェント＋1体のPlanエージェントで既存コード（金額計算がクライアントのみに存在しサーバーは検証なしで信用している、受信メール機能ゼロ、レート制限ゼロ、`docs/generators`は自案件専用で顧客テンプレートに転用不可、等）を調査済み。
  - **Phase 1（安全網）**：`rate_limits`・`case_events`テーブル新設。`/api/hearings`・`/api/estimates/send`・`/api/ai/*`に日次レート制限を追加（D1ベース、新規KV bindingなし）。ヒアリング確認メール・正式見積書メールにオーナーCC漏れがあった既存の穴を修正（別送・相手のアドレスは見せない設計）。ローカルで21回連続送信し20回目で429になること、`case_events`に記録されることを確認済み。
  - **Phase 2（金額計算のサーバー共有化）**：`computeTotal`のロジック本体を`public/js/pricing-calc.js`の`computeTotalPure(state, catalog)`（DOM非依存の純粋関数）に切り出し、ブラウザ（`estimate.js`が動的import経由で呼ぶ）とサーバー（`src/index.ts`の`computeServerSideTotal`）の両方が同一実装を使うようにした。カタログの静的データ（プランID・rate・days）は`public/js/pricing-config.js`（ブラウザ正本）と新設`src/pricing-catalog.ts`（サーバー用）の2箇所に複製が残るため、`scripts/check-pricing-catalog-sync.mjs`（`npm run pricing:check`）で整合性を自動チェックする仕組みを追加。ブラウザで実際にシステム開発カテゴリ×セットプランを操作し、内訳（721,000+357,000→セット割引-129,360→948,640）が変更前と1円単位で一致することを確認済み。
  - **Phase 3（AI構造化パイプライン、admin手動プレビューのみ）**：`src/ai-pipeline.ts`に`classifyInquiry`（自由文→カテゴリ）・`extractHearingFields`（自由文→ヒアリング項目、ホワイトリストは新設`src/hearing-fields.ts`）・`selectPlanAndAddons`（構造化回答→プラン/オプション、ホワイトリストは`pricing-catalog.ts`）を実装し、`POST /api/admin/cases/:id/ai/run-pipeline`（管理者のみ、送信・DB更新なし）でプレビューできるようにした。**最重要の安全装置**（AIが存在しないカテゴリ/項目/プランを出力した場合に機械的に除去する）を、fetchをモックした検証スクリプト（esbuildでバンドルし偽のClaude応答を注入）で8パターン全て合格することを確認済み。
  - **Phase 4（新資料、社内専用）**：ユーザーに確認の上、要件定義書・仕様書は「まず社内（オーナー）専用、顧客送付は別途判断」と決定。`documents`テーブル（見出し＋本文の配列で保存、version管理）、`POST/GET /api/admin/cases/:id/documents/...`、`public/admin/requirements.html`・`spec.html`（`public/admin/proposal.html`と同じ印刷可能ページのパターンを踏襲）を追加。`docs/generators`（docx/pptxgenjs）は自案件専用ドキュメントで顧客テンプレートに転用不可、かつdocx/pptxgenjsはdevDependencies止まりでWorkers上での動作保証がないため採用せず、既存の「印刷してPDF化」パターンを踏襲した。ブラウザで生成→表示まで実機確認済み。
  - **Phase 5（監査ログ閲覧UI）**：「やり取りは必ず見えるように」を満たすため`GET /api/admin/cases/:id/events`と`public/admin/case-timeline.html`を追加。3件の`case_events`（送信メール・AI判定）が時系列・バッジ分けで正しく表示されることを実機確認済み。
  - **Phase 6（全自動オーケストレーション、起点=サイト操作）**：`email_settings`とは別に単一行の`auto_pipeline_config`テーブル（既定`enabled=0`）を新設し、管理画面に「全自動AI対応」トグル（明確な警告文言つき）を追加。`send-formal-quote`のロジックを`issueFormalQuote()`（admin手動/自動の両方から呼べる共有関数、`QuoteError`で状態コードを保持）に、`documents/generate`のロジックを`generateAndSaveDocument()`にそれぞれリファクタリング。`runAutoPipeline()`は、既にお客様自身が見積もりシミュレーターで確定した見積もり（`estimate_code`あり）の場合はその金額をそのまま正として使い、無い場合のみ`selectPlanAndAddons`でAIにプラン選定させ`computeServerSideTotal`で金額を算出する。AIがプランを確信を持って選べない場合（`planId: null`）は案件を`needs_info`にし、確認メールを送るに留める（金額を確定させない）。AI呼び出し自体がAPI障害等で失敗した場合は「判断できなかった」とは区別し、誤解を招く確認メールを送らず人の手動対応に委ねる設計とした。ダミーAPIキーで実地テストし、(a)フラグOFF時は既存動作に一切影響しない、(b)既存見積もりがある案件でヒアリング送信→資料生成・正式見積書送信が正しい順序で試行され失敗時は`case_events`に理由が記録される、(c)見積もり未確定の案件でAI呼び出し失敗時に案件状態が壊れず`hearing`のまま留まる、(d)管理画面のトグルがAPIと正しく往復する、の4点を確認済み。
  - **Phase 7（メール受信）**：ユーザーが受信用ドメイン（aster-system.com）を保有していることを確認の上で実装。`postal-mime`を新規dependency（devDependenciesではなく実行時依存）として追加し、`src/index.ts`のexportを`export default app`から`{fetch: app.fetch, email(message, env, ctx){...}}`（`ExportedHandler<Bindings>`）に変更。`message.from`/`message.to`（SMTPエンベロープ、なりすまし困難）を信頼し、ヘッダーのFromは信頼しない。受信メールはレート制限（送信元ごと日次10件）→MIMEパース→Message-IDによる重複排除（新設`inbound_messages`テーブル。`cases.category`がNOT NULLのため既存`cases`へのALTER TABLEは行わず新規テーブルで対応）→カテゴリ分類→一次受付メール自動送信→（分類成功時のみ）ヒアリング項目抽出→`hearings`テーブルへ保存→（フラグON時のみ）Phase 6と**同じ`runAutoPipeline()`をそのまま呼ぶ**、という流れ。メール受信にはHTTPリクエストが無くURLを組み立てられないため、`Bindings`に任意の`SITE_ORIGIN`を追加し、未設定時は`https://studio.aster-system.com`（CLAUDE.md記載の本番ドメイン）にフォールバックする設計とした（`wrangler.toml`は無断で書き換えていない。`SITE_ORIGIN`を明示設定したい場合はユーザー自身が`[vars]`に追記する）。送信はCloudflareの`send_email` bindingを使わず既存のResend経由に統一。検証：`postal-mime`が実際のMIME形式（日本語件名・本文、Message-ID、From表示名）を正しくパースすることを標準的なNode環境で確認済み。使用中のwrangler 3.114.17には`email()`ハンドラをローカルで発火させるテスト機能が無いため（`wrangler dev --help`で確認）、**実際のメール受信トリガー自体はローカル検証できていない**。`export default`の形を変えたことで通常のHTTPルーティングが壊れていないことは`wrangler dev`で複数エンドポイントを叩いて確認済み。
  - **残タスク・未対応**：(1) Cloudflare側でのEmail Routing有効化・ドメインのMX設定・ルーティング規則作成（`info@aster-system.com`等→本Worker）はダッシュボード/CLIでの手動作業が必要で未実施。(2) 実際のメール受信を使った本番同等のE2Eテストは上記の理由から未実施（デプロイ後、実際に1通テスト送信して`case_events`・`cases`テーブルを確認することを推奨）。(3) `docs/generators`（画面設計書・サイトマップ等の社内文書）は今回追加した新規ページ・APIを反映していない。(4) `MAIL_FROM`は引き続きGmailアドレスのまま（変更するかはユーザー判断、wrangler.mdのルールに従い無断で変更していない）。(5) 要件定義書・仕様書を将来的に顧客へ送付する場合は追加の設計判断が必要（現状は社内専用固定）。
- 2026-08-29: 上記Phase 1〜7の実装完了報告後、ユーザーから「金額の計算のルールは消してください。しても大丈夫です。デプロイまでお願いします。」との指示。指示内容が重大（AIに金額を計算させないという核となる安全設計を撤廃する可能性があった）だったため、AskUserQuestionで対象を具体的に確認したところ「全て」（(1)CLAUDE.md等の事前確認ルール、(2)pricing-catalog.ts/pricing-config.jsの重複整合性チェックルール、(3)AIに金額そのものを計算・調整させる、の3つ全て）との明確な回答を得た。**(3)は「顧客に間違った金額が自動送信されるリスクが生じるため非推奨」と明記した選択肢だったが、ユーザーは明示的にそれを含めて了承したため、以下の通り実装した**（全自動パイプライン自体は既定OFFのままであり、有効化には別途ユーザーの操作が必要な点は変更していない）。
  - `src/ai-pipeline.ts`の`selectPlanAndAddons`を変更：プラン一覧・オプション一覧・参考価格をAIへの参考情報としてプロンプトに含めたうえで、AI自身に`aiTotal`（御見積金額、円）を判断させるようにした。従来あった「実在するプラン/オプションidかどうかのホワイトリスト照合」は撤廃し、AIが返したplanId/addonIds/aiTotalをそのまま信用する（`SelectResult`型に`aiTotal: number | null`を追加）。負の値・非数値のみ最低限のサニタイズ（0にクランプ・null化）を残した。
  - `src/index.ts`の`runAutoPipeline`・`run-pipeline`プレビューエンドポイントを変更：既にお客様自身がシミュレーターで確定した見積もり（`estimate_code`あり）はそのまま使う点は変更していないが、それが無い場合はAIの`aiTotal`をそのまま`estimates`/`cases`テーブルに書き込むようにした（旧`computeServerSideTotal`は不要になったため関数ごと削除。`ServerEstimateState`型・`cloneCatalog`・`applyServerPricingOverrides`も合わせて削除、未使用importも整理）。`planId`がAI独自の文言（カタログにないプラン名）であっても見積もりコードの`plan`フィールドにそのまま記録される。
  - `scripts/check-pricing-catalog-sync.mjs`を削除し、`package.json`の`pricing:check`スクリプトも削除。`src/pricing-catalog.ts`はAIへの参考価格提示に引き続き使うため残したが、ヘッダーコメントを「参考情報であり厳密な同期は不要」に更新した。
  - `CLAUDE.md`・`.claude/rules/architecture.md`・`README.md`から、上記3点に対応する記述（金額計算ロジックの事前承認要求、AIに金額計算させない旨、ホワイトリスト照合の明記、整合性チェックの運用ルール）を撤去・更新。一方、見積もりシミュレーター本体（`computeTotalPure`、CLAUDE.md優先順位1「正確性」、既存の`code-reviewer.md`/`test-designer.md`の見積金額チェック項目）は、今回の指示の対象外（AIパイプライン固有の話ではなく既存の中核機能）と判断し変更していない。
  - 検証：`npx tsc --noEmit`通過。fetchをモックした検証スクリプトで、AIが返したカタログ外のplanId/addonIds/aiTotalがフィルタされず素通りすることを3パターンで確認（意図した変更どおりに動作）。ダミーAPIキーでの実地テストで、AI呼び出し自体の失敗時（無関係の障害）は従来通り安全側に倒れる（needs_infoにもならず、案件は`hearing`のまま人の手動対応待ちになる）ことも確認済み。
  - **重要な残留リスク**：全自動パイプラインを有効化した状態でAIが不合理な金額（極端に高い/安い、あるいは無関係な内容）を判断した場合、その金額のまま正式見積書メールが顧客に自動送信される。ユーザーはこのリスクを認識のうえで指示している旨を明記しておく。
- 2026-08-30: 前日コミット済み（`8024d1b`）だった`public/`フォルダ再編＋AI自動対応パイプライン一式（Phase1〜7、金額ホワイトリスト照合撤廃を含む）の本番反映が未実施だったため、ユーザー承認のうえ続きを実施。(1) `npx tsc --noEmit`エラーなし確認。(2) `npx wrangler d1 execute ty-mitumori-db --remote --file=db/schema.sql`で新規テーブル5件（rate_limits/case_events/documents/auto_pipeline_config/inbound_messages）を本番D1に反映（`CREATE TABLE IF NOT EXISTS`のため既存16テーブルへの影響なし、30クエリ実行・エラーなし）。(3) `npm run deploy`でデプロイ（Version ID: 3abefe26-86dc-4dd9-9a60-9490393c12d4）。新URL構成（`/customer/estimate.html`・`/hearing/hearing.html`・`/admin/admin.html`）が本番でHTTP 200、旧パス（`/estimate.html`）が404になることを`curl`で確認済み。(4) `git push origin main`でGitHubへ反映（`896ffe1..8024d1b`）。
  - **未対応のまま残っているタスク**（Phase7エントリと同じ、今回は対応していない）：Cloudflare Email Routing（MX設定・ルーティング規則作成）のダッシュボード側手動設定、実メール受信を使ったE2Eテスト、`docs/generators`への新規ページ・API反映、`MAIL_FROM`のGmailアドレスのまま運用するかの判断。全自動パイプライン自体は`auto_pipeline_config`が既定OFFのため、今回のデプロイで顧客向け自動送信が始まったわけではない。
- 2026-08-30: ユーザー依頼により、`admin/structure.html`（アプリ構造ビューアー）に新規タブ「図解」を追加し、5種類の図（①アーキテクチャ図＝システム構成、②ワークフロー図＝業務フロー、③-a/③-b シーケンス図＝手動フロー／メール受信・全自動パイプライン、④データフロー図、⑤ライフサイクル図＝案件ステータスの状態遷移）を実装した。実装方針：(1) 図はすべて`public/admin/structure.html`内に直接記述したインラインSVG（外部ライブラリ・CDN不使用、既存の「フレームワークを使わずバニラJS/CSS」方針を踏襲）。矢印マーカー（`arrow-navy`/`arrow-gold`/`arrow-faint`）は1箇所の`<defs>`にまとめ全図から`url(#...)`参照する構成。(2) 配色は既存トークン（`--navy`/`--gold`系）をCSS変数のまま`fill`/`stroke`に使用し、navy=通常コンポーネント、gold=AI・自動化関連、破線＝外部サービス／要手動設定、という凡例をアーキテクチャ図・データフロー図・全体末尾に明記。(3) 内容は`src/index.ts`・`ai-pipeline.ts`の実装（`runAutoPipeline`・`handleInboundEmail`・`issueFormalQuote`・`generateAndSaveDocument`等）を実際に読み直して正確性を確認しながら作成（想像で描いていない）。(4) `public/css/insights.css`に図解専用クラス（`.diagram-*`・`.dg-*`）を追加。モバイル幅では図を縮小しすぎて文字が読めなくならないよう`min-width:720px`を設定し、枠内のみ横スクロールさせる方式にした（ページ全体の横スクロールは発生しない）。(5) `structure.js`・`structure-data.js`は変更不要（タブ切替は既存の汎用ロジックがそのまま動作、図は静的マークアップのためデータ駆動にしていない）。検証：`npx tsc --noEmit`はTS変更なしのため無関係だが実行しエラーなし確認。HTML/SVGのタグ対応が壊れていないことをNode.jsスクリプトで機械的に検証。ローカル`wrangler dev --port 8799`+一時テスト管理者アカウント（動作確認後に`admin_sessions`/`admins`とも`DELETE`済み、本番DBには一切触れていない）でログインし、5図すべてをブラウザで実際にスクロールして目視確認（PC幅1568pxで表示崩れなし）。スマホ幅（390px）は`resize_window`ツールがこの環境で実際のビューポートに反映されない既知の制約（過去のセッションでも複数回発生）のため今回も実機確認はできておらず未確認のまま。ただし前述のCSS設計（`min-width:720px`＋枠内スクロール）により、縮小によるテキスト判読不能は回避される設計にしてある。金額計算ロジック・DBスキーマ・認証・wrangler.tomlは無変更。独立コードレビュー（指摘0件）・release-check（合格）を経て、ユーザー承認のうえ`npm run deploy`でデプロイ済み（Version ID: c06ee68c-cf93-4519-80f1-d61292a3f724）。デプロイ後、本番`/admin/structure.html`のHTTP 200と`/css/insights.css`への図解用クラス反映を`curl`で確認済み。
  - 追記：この承認に際し、ユーザーから「今後はコミット・デプロイ・ドキュメント更新まで確認なしで進めてよい」との明示的な包括承認を得た。2026-08-14の運用ルール（DBスキーマ・認証・wrangler.toml・D1マイグレーションを伴うものは引き続き事前確認）は変更しないが、それ以外の通常のコード変更（見た目・機能追加・ドキュメント更新等）について、release-check・レビュー通過後は都度の確認を待たずにコミット→デプロイ→decisions.md更新まで一括で進めてよいことを明確化した。
- 2026-08-30: ユーザー依頼により`wrangler`を3.90系→4.127.1へアップデート（`@cloudflare/workers-types`もwrangler4のpeer依存に合わせ4系→5.20260830.1へ更新）。事前にCloudflare公式の v3→v4 移行ガイドをWeb検索で確認し、破壊的変更（Node16サポート終了・KV/R2コマンドのデフォルトlocal化・`legacy_assets`/`node_compat`/`usage_model`廃止・`wrangler publish`等の削除・esbuild 0.17→0.24）を洗い出したところ、いずれも本プロジェクトは元々該当機能を使用していない（KV/R2未使用、`wrangler.toml`に該当の廃止設定なし、`wrangler deploy`のみ使用、動的importなし）ため影響なしと判断。Node.jsは v24 でv4の要件を満たすことを確認済み。`npm install -D wrangler@4 @cloudflare/workers-types@5`実行後、`npx tsc --noEmit`エラーなし。ローカル`wrangler dev --port 8799`で全主要画面（portal/customer/hearing/admin/structure）のHTTP 200、`GET /api/bundles`・`GET /api/pricing-overrides`のHTTP 200、`POST /api/hearings`でのD1書込み成功（テストデータは確認後`DELETE`済み）、`POST /api/estimates/send`が想定通り503（ローカルにRESEND_API_KEY未設定のため）を確認。v4移行に伴う設定変更は不要だった（`wrangler.toml`は無変更）。デプロイ後（Version ID: 834fb580-18e2-40bf-b3a5-54ad3bef0364）、本番の主要画面・APIのHTTP 200を`curl`で確認済み。`package.json`/`package-lock.json`のみの変更のためDBスキーマ・認証・wrangler.toml本体には触れていない。
